Place your background image here as `background.jpg`.

The HTML template references the file at `/static/background.jpg`.

Suggested filename and size:
- Name: background.jpg
- Recommended resolution: 1920x1080 or larger for best quality
- Keep file size under ~2-5MB for faster page loads

If you want a different filename, update `templates/index.html` background URL accordingly.
